version: 0.1
frontend:
  phases:
    preBuild:
        commands:
          - cd Frontend
          - npm ci
    build:
        commands:
          - npm run build
  artifacts:
    baseDirectory: ./Frontend/build
    files:
      - '**/*'
  cache:
    paths:
      - node_modules/**/*
